import classes from '../../board-toolbar.module.scss';

export interface ClearPromptProps {
    onGenerateImage: (image: ImageData) => void
}

export const ClearPrompt = (props: ClearPromptProps) => {
	
	return (
		<div></div>
	);
};